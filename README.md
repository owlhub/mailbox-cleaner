# mailbox-cleaner

Copy `.env.local` file and create new file named `.env`.

Update variables in .env file

Install dependencies
```shell
npm install
```

or

```shell
yarn
```

run script using

```shell
node index.js
```
